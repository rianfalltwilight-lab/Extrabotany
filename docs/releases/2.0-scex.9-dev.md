# 2.0-scex.9-dev：亚空间长矛模型与碰撞箱对齐

修正旧版亚空间长矛渲染中遗留的方块实体平移，让模型中心与实体碰撞箱中心对齐；碰撞箱尺寸由 0.35 调整为 0.5。Minecraft 的轴对齐碰撞箱不随模型旋转，长轴命中继续沿用每 tick 运动段扫掠。

本轮只修改长矛实体、长矛渲染器、对应几何验证探针及版本号。兼容层仍为 1.4.2，无需更新桥接 JAR；其与本候选的配对验证已完成。

## 冻结候选验证

- 离线构建与 JAR 审计通过；696 类、1536 JSON、366 PNG、14 OGG，资源审计 errors=[]，运行 JAR 未混入开发探针。
- GameTest 第一次为 37/38，guardian phase 雾效计数预期 64、实际 59；保留失败记录。同一候选在独立运行中重测 38/38，通过后正常退出。
- 真实客户端 368/368 断言通过，包含新增的两项长矛几何断言，保留 10 张截图并正常退出。
- 完整整包、多人、着色器、长期性能与生产环境未重新验收。

公开 JAR 的类文件与游戏资源保持冻结候选字节，只补充公开许可证、署名和构建说明。发行包包含运行 JAR、sources JAR、源码 ZIP 及 SHA-256 清单。客户端与服务端应使用同一版本。
